Olá<%= @user.name.present? ? ", #{@user.name}" : "" %>,

Recebemos um pedido para redefinir a senha da sua conta no Fundo da Escola.

Abra este link para escolher uma nova senha:
<%= @reset_url %>

Este link expira em <%= @expires_in %>. Se você não pediu isso, ignore este e-mail.
