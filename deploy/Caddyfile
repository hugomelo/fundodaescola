# Automatic HTTPS via Let's Encrypt. Point the DNS A record for
# api.fundodaescola.com.br at this VPS (vps.prout.io) before starting the stack.
{$API_DOMAIN} {
	encode gzip
	reverse_proxy api:3000
}
